apiVersion: apps/v1
kind: Deployment
metadata:
  name: service2
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: service2
  template:
    metadata:
      labels:
        app: service2
    spec:
      containers:
      - name: service2
        image: index.docker.io/himanshusarma123/service2:v3
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: service2-monitor
  namespace: default          # Keep it in the same namespace as your app
  labels:
    # CRITICAL: This label MUST match the release name of your prometheus stack
    release: monitoring-stack
spec:
  selector:
    matchLabels:
      app: service2
  endpoints:
  - port: http                
    path: /metrics            
    interval: 10s     
---
apiVersion: v1
kind: Service
metadata:
  name: service2-svc
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: service2
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
---