apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: argocd1-ingress
  namespace: default
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
  - host: apihimanshu324.duckdns.org
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: auth-svc
            # name: k8s-automation-svc
            port:
              number: 8000
  - host: service2himanshu324.duckdns.org
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: service2-svc
            port:
              number: 8000