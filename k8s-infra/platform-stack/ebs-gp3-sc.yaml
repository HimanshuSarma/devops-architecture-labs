# apiVersion: storage.k8s.io/v1
# kind: StorageClass
# metadata:
#   name: ebs-gp3 # 'gp3' is the current AWS standard for general purpose
#   annotations:
#     storageclass.kubernetes.io/is-default-class: "true" # This makes it the automatic choice
# provisioner: ebs.csi.aws.com
# parameters:
#   type: gp3
#   fsType: ext4
# reclaimPolicy: Retain
# volumeBindingMode: WaitForFirstConsumer