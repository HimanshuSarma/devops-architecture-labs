# apiVersion: apps/v1
# kind: Deployment
# metadata:
#   name: auth
#   namespace: default
# spec:
#   replicas: 2
#   selector:
#     matchLabels:
#       app: auth
#   strategy:
#     type: RollingUpdate
#     rollingUpdate:
#       maxUnavailable: 0
#       maxSurge: "25%"
#   template:
#     metadata:
#       labels:
#         app: auth
#       annotations:
#         # This tells Prometheus to scrape this pod
#         prometheus.io/scrape: "true"
#         # The port your application code exposes the /metrics endpoint on
#         prometheus.io/port: "8000" 
#         # The path where your app serves metrics
#         prometheus.io/path: "/metrics"
#     spec:
#       shareProcessNamespace: true
#       containers:
#       - name: auth
#         image: index.docker.io/himanshusarma123/auth:v12
#         ports:
#         - containerPort: 8000
#         resources:
#           requests:
#             memory: "64Mi"
#             cpu: "100m"
#           limits:
#             memory: "128Mi"  # The container will immediately get terminated if it crosses this
#             cpu: "200m"

#         livenessProbe:
#           httpGet:
#             path: /       # A lightweight endpoint to see if the process is responsive
#             port: 8000
#           initialDelaySeconds: 5  # Give the app 5 seconds to boot up before checking
#           periodSeconds: 10       # Check every 10 seconds

#         readinessProbe:
#           httpGet:
#             path: /healthz/readiness # Hit an endpoint that confirms your internal dependencies/RBAC work
#             port: 8000
#           initialDelaySeconds: 5  # Start checking 5 seconds after boot
#           periodSeconds: 10       # Check every 10 seconds
#           failureThreshold: 5
---
  apiVersion: argoproj.io/v1alpha1
  kind: Rollout
  metadata:
    name: backend-rollout
  spec:
    replicas: 1
    revisionHistoryLimit: 1
    selector:
      matchLabels:
        app: backend
    template:
      metadata:
        labels:
          app: backend
      spec:
        containers:
        - name: backend
          image: himanshusarma123/ai-monitoring-service-backend:7cc5101
          ports:
          - containerPort: 8000
    strategy:
      blueGreen: 
        activeService: backend-prod-svc
        previewService: backend-staging-svc
        autoPromotionEnabled: false
        scaleDownDelaySeconds: 30
        previewReplicaCount: 1
---
# apiVersion: monitoring.coreos.com/v1
# kind: ServiceMonitor
# metadata:
#   name: backend-service-monitor
#   namespace: default          # Keep it in the same namespace as your app
#   labels:
#     release: monitoring-stack
# spec:
#   selector:
#     matchLabels:
#       app: auth-metrics-group
#       role: production
#   endpoints:
#   - port: http                
#     path: /metrics            
#     interval: 10s             
# ---
apiVersion: v1
kind: Service
metadata:
  name: backend-prod-svc
  namespace: default
  labels:
    app: backend-metrics-group
    role: production
spec:
  type: ClusterIP
  selector:
    app: backend
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: backend-staging-svc
  namespace: default
  labels:
    app: backend-metrics-group
    role: staging
spec:
  type: ClusterIP
  selector:
    app: backend
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000