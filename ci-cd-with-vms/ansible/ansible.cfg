[defaults]
# 🟢 Change this from hosts.ini to your dynamic config file
inventory = aws_ec2.yaml
host_key_checking = False
interpreter_python = auto_silent

# 🟢 CRUCIAL: You must explicitly enable the auto-parsing plugin block
[inventory]
enable_plugins = amazon.aws.aws_ec2, host_list, script, yaml, ini